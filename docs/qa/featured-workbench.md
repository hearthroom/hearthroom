# 精選卡工作台修正

2026-09-22。需求：精選操作放在社群管理工作台，只讓有效精選管理員看到；修正資格載入，發布後唯讀驗證。

驗收範圍：`FEATURED-01`。`/review/cases` 可見管理員專屬捷徑；`/review/cards` 選卡後標記／取消，名額與結果重新讀回。普通審核員、不同平台與過期資格不露出操作。公開卡片頁只有徽章。沒有修改正式帳號權限、返點規則或真實卡片。

## 回歸證據

- Red：原 store 缺少 featured；管理頁未顯示按鈕；D1 管理 projection 缺 provider／featured；登入刷新丟錯未清掉資格。各自先失敗再修復。
- mounted component：管理員標記／取消、普通審核員隱藏、跨平台隱藏、確認取消、換帳號中止、錯誤可重試、額度耗盡。
- Store：資格載入與拒絕、登出、遲到回應、登入刷新失敗、平台切換。
- Worker 真實 D1：管理列表／詳情的持久精選值；既有精選測試保留非審核員、非管理員、配額、卡片資格與供應商拒絕。
- Chrome 合成旅程：處置複核 → 精選卡管理 → 選卡 → 確認標記 → 名額 0→1、取消按鈕與徽章 → 確認取消 → 名額 1→0。手機 390px 工作台可讀；新捷徑保持觸控高度。

UI 合成資料只驗前端流程；不當作正式資料寫入證明。正式卡片不做標記測試。公開報告不含帳號識別、真卡內容或截圖。

## 視覺與權限驗收補充

- 真實 Chrome：一般審核員在處置複核沒有精選捷徑，選卡後也沒有精選控制，原有理由與處置欄仍存在。
- 390px：繁中、英文確認框完成實際閱讀與取消；日文、韓文、簡中選卡後控制均可見且沒有文字裁切或水平溢出。新入口與操作按鈕均為 44px。
- 幾何探針實際抓到新捷徑原為 36px 的不足，修正後讀回 44px；此為實際負樣本及修正回歸。共用舊控制不宣稱此次全數調整。
- 原生瀏覽器工具部分 DOM 請求逾時；以同一分頁的 CDP 唯讀診斷回復讀取，沒有換成 headless 驗收。
- 本地完整套件已通過 Worker 554 與 web 492 項；web 1 個需要私有真卡資料的 opt-in suite skipped。型別及完整建置通過；既有大 bundle、字型與 Sass 警告保留。測試預期中的失敗分支會輸出 localhost:3000 拒絕連線診斷，runner 最終沒有測試失敗或未處理錯誤。
- 發布前 main 同步帶入其他工作線的 stage／Android 更新；只同步其既有提交，沒有修改那些功能。每次同步後核對固定 stage 與完整可信測試，再以正常 push 發布。
